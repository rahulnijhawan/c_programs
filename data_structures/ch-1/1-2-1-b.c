/*
	the mode of an array of numbers is the number m in the array that is repeated most frequently.
	 If more than one numberis repeated with equal maximal frequencies, there is no mode.
	  Write a C function that accepts an array of numbers and returns the mode or an indication 
	  that the mode does not exist.
*/